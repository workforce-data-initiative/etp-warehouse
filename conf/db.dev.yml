# Database connection parameters

sqlite:
  transactional:
    database: /databases/tpot.db

postgresql:
  transactional:
      host: localhost
      port: 5433
      username: postgres
      password: postgres
      database: tpot