# Database connection parameters

sqlite:
  database: /Users/akebirungi/Databases/tpot.db

postgresql:
    host: localhost
    port: 5432
    username: postgres
    password: postgres
    database: tpot